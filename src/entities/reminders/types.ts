export enum E_Priority_Reminders {
  critical = 'critical',
  high = 'high',
  medium = 'medium',
  low = 'low',
}

export enum E_Status_Reminders {
  wait = 'wait',
  done = 'done',
  overdue = 'overdue',
}

export enum E_Types_Actions {
  to = 'to',
  plan = 'plan',
  repair = 'repair',
  other = 'other',
}
